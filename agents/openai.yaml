interface:
  display_name: "Punk Town"
  short_description: "Operate every Punk Town user flow through Bankr"
  default_prompt: "Use $punktown to inspect or operate Punk Town on Base from my active Bankr EVM wallet. Verify the reviewed deployment, plan and decode every unsigned transaction, use exact approvals, confirm each fresh economic step, then require mined receipts, expected events, and fresh state postconditions."
